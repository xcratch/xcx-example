{
  "name": "xcx-example",
  "version": "0.1.1",
  "description": "example extension for Xcratch",
  "scripts": {
    "register": "npx xcratch-register --link -C --id=xcratchExample --gui=\"../scratch-gui\"",
    "build": "npx xcratch-build --module=xcratchExample --gui=\"../scratch-gui\""
  },
  "devDependencies": {
    "eslint-config-scratch": "^7.0.0"
  }
}
